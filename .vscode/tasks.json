{
    "version": "2.0.0",
    "tasks": [
        {
            "command": "pandoc",
            "label": "Pandoc: convert to HTML",
            "isBackground": false,
            "type": "shell",
            "args": [
                "${fileBasename}",
                "-f",
                "markdown",
                "-t",
                "html",
                "--standalone",
                "-o",
                "${fileBasenameNoExtension}.html"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ]
}