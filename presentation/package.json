{
    "name": "day-path-blog",
    "repository": {
        "type": "git"
    },
    "version": "0.0.1",
    "scripts": {
        "build": "npx eleventy",
        "markdownlint": "npx markdownlint --output markdownlint-output.txt entry/**/*.md"
    },
    "devDependencies": {
        "@11ty/eleventy": "^0.10.0",
        "markdownlint-cli": "^0.22.0",
        "moment": "^2.27.0"
    }
}
